<h1>Deven's Personal Project Portfolio</h1>

{{#each projects as |project|}}
  <div class="project">
    <div class="project-name">
      <a href="/projects/{{project.name}}">
         {{project.name}}
       </a>
    </div>

    
  </div>
{{/each}}
