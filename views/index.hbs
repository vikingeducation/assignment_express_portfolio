<h1>My Projects</h1>

{{#each projects as |project|}}
  <div class="project">
    <div class="project-name">
      <a href="/projects/{{project.name}}">
        {{project.displayName}}
      </a> - Tech used: [ <span class="project-technologies">{{project.technologies}}</span> ]
    </div>

  </div>
{{/each}}